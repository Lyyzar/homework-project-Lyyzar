package rokafogo.model;

/**
 * Egy enum osztály, amivel a bábujainkat jelöljük.
 * A ONE a fehér parasztunk.
 * A MULTIPLE a fekete parasztaink.
 * A NONE pedig az üres mezőink.
 */
public enum Pawn {
    ONE,
    MULTIPLE,
    NONE
}
